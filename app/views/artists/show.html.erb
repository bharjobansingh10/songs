<h1><%= @artist.name %></h1>
<h2>Albums:</h2>
<ul>
  <% @artist.albums.each do |album| %>
    <li><%= link_to album.title, album_path(album) %></li>
  <% end %>
</ul>
<%= link_to 'Back', artists_path %>
